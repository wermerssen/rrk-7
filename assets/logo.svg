<svg xmlns="http://www.w3.org/2000/svg"
     width="320" height="80"
     viewBox="0 0 320 80">

  <!-- Hintergrund transparent, kein <rect> nötig -->

  <!-- Formel-Text -->
  <text x="20" y="50"
        font-family="Times New Roman, serif"
        font-size="32"
        fill="black">
    R = β C
  </text>

  <!-- Exponent 7 -->
  <text x="205" y="32"
        font-family="Times New Roman, serif"
        font-size="20"
        fill="black">
    7
  </text>

  <!-- Rahmen (optional, wie boxed-Formel) -->
  <rect x="10" y="15" width="300" height="50"
        fill="none"
        stroke="black"
        stroke-width="1" />
</svg>
