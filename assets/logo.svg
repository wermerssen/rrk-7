<svg xmlns="http://www.w3.org/2000/svg"
     width="360" height="90"
     viewBox="0 0 360 90">

  <!-- Formel -->
  <text x="20" y="60"
        font-family="Times New Roman, serif"
        font-size="40"
        fill="black">
    R = β C
  </text>

  <!-- Exponent 7: enger an C und leicht tiefer -->
  <text x="198" y="38"
        font-family="Times New Roman, serif"
        font-size="26"
        fill="black">
    7
  </text>

  <!-- Rahmen (optional) -->
  <rect x="10" y="20" width="340" height="50"
        fill="none"
        stroke="black"
        stroke-width="1" />
</svg>
